-- Check Current Data in Training Tables
-- Let's see what data exists and what column names are used

-- Check training_data contents
SELECT * FROM training_data LIMIT 5;

-- Check assistant_voice_prefs contents  
SELECT * FROM assistant_voice_prefs LIMIT 5;

-- If logic_notes and reference_materials have data, check them too
-- SELECT * FROM logic_notes LIMIT 5;
-- SELECT * FROM reference_materials LIMIT 5;
